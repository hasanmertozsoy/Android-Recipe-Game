<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/hmo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hmo"
        android:supportsRtl="true"
        android:theme="@style/Theme.VizeUygulaması"


        tools:targetApi="31">
        <activity
            android:name=".AcilisEkrani"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".GirisEkrani"
            android:exported="true"/>


        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"/>


        <!-- Yemek Detay Activity -->
        <activity
            android:name=".YemekDetaylari"
            android:exported="true"/>


        <!-- Oyun Activity -->
        <activity
            android:name=".OyunEkrani"
            android:exported="true"/>

    </application>

</manifest>